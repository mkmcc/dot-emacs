;; auto-generated
(defconst mu4e-mu-version "0.9.8.5"
  "Required mu binary version; mu4e's version must agree with
  this.")

(defconst mu4e-builddir "/strada1/mkmcc/build/src/mu"
  "Top-level build directory.")

(provide 'mu4e-meta)
